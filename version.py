VERSION = (0, 0, 1, 0)
VERSION_INFO = '.'.join(str(nv) for nv in VERSION)
__version__ = VERSION_INFO

#Vesion history

# 0.0.1.0
# - Transfered exceptions from pullgerReflection
# - Add versionalisation

# 0.0.0.0
# - Initial upload)
